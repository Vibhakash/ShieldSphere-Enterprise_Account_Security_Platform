# FastAPI core
fastapi==0.139.2
uvicorn[standard]==0.32.1
python-multipart==0.0.32

# Pydantic v2 + settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# SQLAlchemy 2.0 async + psycopg3
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# Auth
PyJWT[crypto]==2.13.0
passlib[bcrypt]==1.7.4
pyotp==2.9.0
qrcode[pil]==7.4.2
webauthn==3.0.0

# Redis
redis[hiredis]==5.2.0

# Background jobs
apscheduler==3.10.4

# HTTP client (for external APIs)
httpx==0.28.1
aiohttp==3.14.3
truststore==0.10.4

# GeoIP
geoip2==4.8.1
maxminddb==2.6.2

# Groq SDK
groq==0.13.0

# Password entropy
zxcvbn==4.4.28

# Network tools (for attack simulator)
python-nmap==0.7.1
scapy==2.7.0

# Docker SDK (for sandbox manager)
docker==7.1.0

# WebSocket support (built into fastapi/starlette)
websockets==14.1

# Utilities
python-dotenv==1.2.2
cryptography==49.0.0
Pillow==12.3.0
openpyxl==3.1.5
reportlab==4.4.7
slowapi==0.1.9
structlog==24.4.0
tenacity==9.0.0

# Runtime type compatibility
typing-extensions==4.12.2

# Development and release verification (kept here to maintain one Python manifest)
pytest==9.1.1
pytest-asyncio==1.4.0
pip-audit==2.10.1
